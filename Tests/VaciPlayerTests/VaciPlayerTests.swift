import XCTest
@testable import VaciPlayer

final class VaciPlayerTests: XCTestCase {
    func testExample() throws {
        // Basic test to ensure project compiles
        XCTAssertTrue(true)
    }
}